<?xml version="1.0" encoding="utf-8"?>
<!--
    canRetrieveWindowContent="true" is REQUIRED for onAccessibilityEvent to read
    the node tree and click ThemeManager approval buttons. Without it, the service
    receives events but getRootInActiveWindow() always returns null.

    packageNames restricts events to ThemeManager only (battery-friendly). The
    broadcast receiver for miui.intent.action.CHECK_TIME_UP is unaffected by this
    filter — it receives system-wide broadcasts independently.

    typeWindowContentChanged is needed to detect when ThemeManager's dialog content
    changes (e.g. button text becomes visible after an animation).
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:packageNames="com.android.thememanager,com.miui.thememanager"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="100"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:canRequestTouchExplorationMode="false"
    android:canRequestFilterKeyEvents="false"
    android:settingsActivity="app.hypermtz.MainActivity" />
